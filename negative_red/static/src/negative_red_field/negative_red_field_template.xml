<templates>
  
    <!-- 
    <t t-name="negative_red.NegativeRedTemplate" t-inherit="web.FloatField" t-inherit-mode="primary">
        <xpath expr="//span" position="replace">
            <div>toto <span t-if="props.readonly" t-esc="formattedValue" /> tutu</div>
        </xpath>
    </t> 
    -->

    <!-- 
  <t t-name="negative_red.NegativeRedTemplate">
        <span t-if="props.readonly" t-esc="formattedValue" />
        <input 
            t-else="" 
            t-on-focusin="onFocusIn" 
            t-on-focusout="onFocusOut" 
            t-att-id="props.id" 
            t-ref="numpadDecimal"  
            t-att-placeholder="props.placeholder" 
            t-att-type="props.inputType" 
            inputmode="decimal" 
            class="o_input" 
            autocomplete="off"
            t-att-step="props.step" />
    </t> 
    -->

</templates>
